[package]
name = "sharia-review-manager"
version = "0.1.0"
description = "Sharia compliance review with pluggable ZK/Arcium interface"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "sharia_review_manager"

[features]
default = []
cpi = ["no-entrypoint"]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
idl-build = ["anchor-lang/idl-build"]
anchor-debug = []
custom-heap = []
custom-panic = []

[dependencies]
anchor-lang = { version = "1.0.2", features = ["init-if-needed"] }

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(target_os, values("solana"))'] }
